<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Byte Blog</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <%- include('partials/header') %>
  <main>
    <h1 style="color:#426880 ;">Blog Posts</h1>
    <ul>
      <% posts.forEach(post => { %><br>
        <li>
        <h2 class="post.title"><a href="/post/<%= post.id %>"><%= post.title %></a></h2>
          <form action="/edit/<%= post.id %>" method="post" style="display:inline;">
            <button type="submit"><a href="/edit/<%= post.id %>">Edit</a></button>
          </form>
          <form action="/delete/<%= post.id %>" method="post" style="display:inline;">
            <button type="submit">Delete</button>
          </form>
        </li>
      <% }) %>
    </ul>
  </main>
<br><br>
  <%- include('partials/footer') %>
</body>
</html>
